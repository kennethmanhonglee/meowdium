extends layout.pug

block content
    div(class='feed')
        each post in posts
            div(class='post')
                div(class='post-title')= post.title
                if (post.subtitle)
                    div(class='post-subtitle')= post.subtitle
                div(class='post-info')
                    div(class='post-user')= post.User.userName
                    div(class='post-time')= post.updatedAt

